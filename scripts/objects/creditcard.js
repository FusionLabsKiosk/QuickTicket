function CreditCard(firstName, lastName, number, expMonth, expYear) {
    
    this.firstName = firstName;
    this.lastName = lastName;
    this.number = number;
    this.expMonth = expMonth;
    this.expYear = expYear;
}